<%= form_with(model: protfoilo) do |form| %>
  <% if protfoilo.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(protfoilo.errors.count, "error") %> prohibited this protfoilo from being saved:</h2>

      <ul>
        <% protfoilo.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :social_media, style: "display: block" %>
    <%= form.text_field :social_media %>
  </div>

  <div>
    <%= form.label :user_id, style: "display: block" %>
    <%= form.text_field :user_id %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
